const projects = require("./projects_model")
const resources = require("./resources_model")
const tasks = require("./tasks_model")
const contexts = require("./contexts_model")

module.exports = { projects, resources, tasks, contexts }